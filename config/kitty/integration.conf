# vim:fileencoding=utf-8:ft=kitty
# 外部集成相关配置：这里的值会被脚本和启动参数依赖。

# open-kitty 等脚本依赖 kitten @，所以这里必须保持开启。
allow_remote_control yes
# 保留固定 socket 路径，和 open-kitty / launch services 保持兼容。
listen_on unix:/tmp/mykitty
