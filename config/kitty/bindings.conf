# vim:fileencoding=utf-8:ft=kitty
# 键盘优先工作流。
# 约束：不要使用 Raycast 已占用的 option+shift 前缀。

# 速查表
# - 新窗口：cmd+enter / ctrl+cmd+enter
# - 分屏：cmd+d（竖分） / shift+cmd+d（横分）
# - 搜索：cmd+f / ctrl+cmd+f
# - 切 pane：ctrl+cmd+[ / ctrl+cmd+]
# - 关 pane：ctrl+cmd+w
# - 新 tab：ctrl+cmd+t
# - 切 tab：ctrl+cmd+left / ctrl+cmd+right
# - 看历史：ctrl+cmd+h
# - 上一条命令输出：ctrl+cmd+g
# - 切布局：ctrl+cmd+l
# - 字号：ctrl+cmd++ / ctrl+cmd+- / ctrl+cmd+backspace

# 使用终端专属前缀，尽量避开 macOS 和 Raycast 的常见组合。
kitty_mod cmd+ctrl

# 窗口与分屏。
# 在当前工作目录打开新的 shell。
map kitty_mod+enter new_window_with_cwd
map cmd+enter       new_window_with_cwd
# 保留原有分屏肌肉记忆。
map cmd+d           launch --cwd=current --location=vsplit
map shift+cmd+d     launch --cwd=current --location=hsplit
# 关闭或切换 pane，不依赖鼠标。
map kitty_mod+w     close_window
map kitty_mod+]     next_window
map kitty_mod+[     previous_window
# 重排、调整大小、快速聚焦可见 pane。
map kitty_mod+`     move_window_to_top
map kitty_mod+r     start_resizing_window
map kitty_mod+f7    focus_visible_window
map kitty_mod+f8    swap_with_window

# Tab。
# 创建、关闭、切换 tab。
map kitty_mod+t         new_tab
map kitty_mod+q         close_tab
map kitty_mod+right     next_tab
map kitty_mod+left      previous_tab
# 调整 tab 顺序或手动命名。
map kitty_mod+.         move_tab_forward
map kitty_mod+,         move_tab_backward
map kitty_mod+alt+t     set_tab_title

# 布局与视图。
# 当前分屏不好用时快速切换布局。
map kitty_mod+l         next_layout
# 进入全屏专注模式。
map kitty_mod+f11       toggle_fullscreen
# 字号缩放与恢复默认。
map kitty_mod+equal     change_font_size all +2.0
map kitty_mod+plus      change_font_size all +2.0
map kitty_mod+kp_add    change_font_size all +2.0
map kitty_mod+minus     change_font_size all -2.0
map kitty_mod+kp_subtract change_font_size all -2.0
map kitty_mod+backspace change_font_size all 0
# 提供完全 reset 和只清到当前光标两种清理方式。
map kitty_mod+delete    clear_terminal reset active
map cmd+k               clear_terminal to_cursor active

# 滚动历史与 prompt 导航。
# 看日志时支持按行、按页、首尾移动。
map kitty_mod+up        scroll_line_up
map kitty_mod+down      scroll_line_down
map kitty_mod+page_up   scroll_page_up
map kitty_mod+page_down scroll_page_down
map kitty_mod+home      scroll_home
map kitty_mod+end       scroll_end
# 直接在 shell prompt 之间跳转，不用慢慢滚。
map kitty_mod+z         scroll_to_prompt -1
map kitty_mod+x         scroll_to_prompt 1
# 打开完整 scrollback，或只看上一条命令输出。
map kitty_mod+h         show_scrollback
map kitty_mod+g         show_last_command_output

# 搜索与 hints。
# 保留 cmd+f，同时给 kitty_mod 提供同能力入口。
map kitty_mod+f launch --allow-remote-control kitty +kitten kitty_search/search.py @active-kitty-window-id
map cmd+f       launch --allow-remote-control kitty +kitten kitty_search/search.py @active-kitty-window-id
# 通过 hints 快速选取行、单词、路径和链接。
map kitty_mod+y>y kitten hints --type line --program @
map kitty_mod+y>w kitten hints --type word --program @ --multiple
map kitty_mod+p>f kitten hints --type path --program -
map kitty_mod+p>shift+f kitten hints --type path
map kitty_mod+p>l kitten hints --type line --program -
map kitty_mod+p>h kitten hints --type hyperlink
