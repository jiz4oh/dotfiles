#!/usr/bin/env bash
set -euo pipefail

vim_install="$(cd "{{ .chezmoi.sourceDir }}/.." && pwd)/vim/install"
if [ ! -x "$vim_install" ]; then
  echo "[vim-init][warn] missing executable: $vim_install" >&2
  exit 0
fi
"$vim_install"
